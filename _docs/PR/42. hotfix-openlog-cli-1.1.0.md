## 🔗 관련 이슈
- close: #

---

## 1. PR 유형
- [x] 🐛 버그 수정 (Bug Fix)
- [ ] ✨ 새로운 기능 (New Feature)
- [ ] ♻️ 리팩토링 (Refactoring)
- [ ] 📝 문서 업데이트 (Documentation)
- [x] ⚙️ 설정/빌드 변경 (Chore)

---

## 2. 변경 배경 및 목표
- **변경 전 상태:** MCP Workspace 기능이 `develop`에 반영됐지만 OpenLog CLI 패키지 버전은 이미 npm에 배포된 `1.0.0`으로 유지됐고, Frontend·CLI workflow는 Node.js 20 런타임 기반 `actions/setup-node@v4`를 사용했다.
- **문제/필요성:** `develop` push 후 실행된 OpenLog CLI CI/CD의 `Check version is unpublished` 단계가 중복 버전을 감지해 실패했고 npm publish가 실행되지 않았다. GitHub Actions에서는 Node.js 20 Action을 Node.js 24로 강제 실행한다는 deprecation 경고도 발생했다.
- **이번 PR의 목표:** 기능 추가에 맞춰 CLI 패키지 버전을 `1.1.0`으로 올리고, setup-node Action을 Node.js 24 런타임 기반 버전으로 갱신해 다음 배포가 경고 없이 진행되게 한다.

---

## 3. 주요 구현 내용 및 동작 방식

### A. OpenLog CLI 패키지 버전 갱신
- **관련 위치/대상:** `packages/openlog-cli/package.json`, `packages/openlog-cli/package-lock.json`
- **변경 전 상황:** 두 파일의 루트 패키지 버전이 npm에 이미 게시된 `1.0.0`이었다.
- **변경한 내용:** 기능 추가를 나타내는 minor 버전 `1.1.0`으로 package metadata와 lockfile의 루트 패키지 버전을 함께 갱신했다.
- **변경 후 동작:** OpenLog CLI CI/CD가 `1.1.0`을 미배포 버전으로 확인하고 npm publish 단계를 실행할 수 있다.
- **바뀌는 데이터/상태:** 배포 패키지 식별자가 `@kitae9999/openlog-cli@1.1.0`으로 변경된다.
- **영향 범위:** OpenLog CLI npm 패키지 버전과 `develop` push 시 실행되는 publish workflow.

### B. GitHub Actions setup-node 갱신
- **관련 위치/대상:** `.github/workflows/frontend-ci.yml`, `.github/workflows/openlog-cli-publish.yml`
- **변경 전 상황:** Frontend CI와 CLI 검증·배포 job이 Node.js 20 런타임 기반 `actions/setup-node@v4`를 사용해 deprecation 경고를 출력했다.
- **변경한 내용:** 세 setup-node 사용 지점을 `actions/setup-node@v6`으로 갱신했다. 프로젝트 실행 버전인 Node.js 22·24와 npm·pnpm cache 설정은 유지했다.
- **변경 후 동작:** setup-node Action 자체가 Node.js 24 런타임에서 실행되며 기존 프로젝트 Node.js 버전과 dependency cache 동작은 유지된다.
- **바뀌는 데이터/상태:** GitHub Actions의 setup-node Action major version
- **영향 범위:** Frontend pull request CI, OpenLog CLI 검증 및 npm publish workflow

---

## 4. 스크린샷 (선택)

패키지 버전 및 CI 설정 변경으로 해당 없음.

## 5. 테스트 및 검증 방법
- **검증 환경:** macOS, Node.js 25.2.1, TypeScript 6.0.3
- **검증한 시나리오:** CLI 전체 테스트, TypeScript 빌드, npm tarball 구성 및 버전, npm registry의 기존 배포 버전, workflow의 setup-node 사용 지점과 diff 형식을 확인했다.
- **확인한 결과:** `npm test` 18건 성공, `npm run build` 성공, `npm pack --dry-run`에서 `@kitae9999/openlog-cli@1.1.0` 생성 확인, npm registry에 `1.1.0` 미배포 확인, setup-node 세 곳의 `@v6` 적용 및 `git diff --check` 성공.
- **확인하지 못한 부분:** 실제 npm publish는 `develop` 머지 후 CI/CD에서 실행되므로 로컬에서는 수행하지 않았다.

---

## 6. 리뷰어 참고 사항 (선택)
- **리뷰할 때 봐야 할 점:** 실패한 workflow를 재실행하는 것만으로는 이미 게시된 `1.0.0`을 다시 배포할 수 없으므로 새 버전 커밋이 필요하다.
- **영향 범위:** 패키지 소스나 런타임 동작은 바뀌지 않고 배포 버전 metadata와 GitHub Actions 실행 환경만 변경된다.

---

## 7. 체크리스트
- [x] 코드가 프로젝트의 코딩 스타일 가이드를 준수합니다.
- [x] 변경 사항에 대한 테스트 코드를 작성하거나 통과했습니다.
- [ ] 관련된 문서(README, API 명세 등)를 업데이트했습니다.
